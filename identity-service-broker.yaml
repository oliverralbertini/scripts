session_name: local server
suppress_history: false
windows:
- window_name: local server
  layout: tiled
  shell_command_before:
    - cd ~/workspace/identity-service-broker
  panes:
    - shell_command: mvn spring-boot:run
    - shell_command:
      - cd react-dashboard
      - npm start
